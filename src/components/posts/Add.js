import React from 'react';

let Add = ({addPost}) => {


    return(
        <div>
            <input />
            <button onClick={
               null
            }>send</button>
        </div>

    )
}

export default Add;